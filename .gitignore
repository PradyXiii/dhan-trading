# Secret credentials - never upload these
.env

# Python cache files - not needed in version control
__pycache__/
*.pyc
*.pyo

# Data files generated during backtest
*.csv
*.json
data/

# Local-only scripts (VM only, never published)
portfolio_tracker.py
refresh_token.py
logs/
